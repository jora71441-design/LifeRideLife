name: Update Catalog Data

on:
  schedule:
    # Запуск каждые 6 часов (автоматически)
    - cron: '0 */6 * * *'
  # Возможность запустить вручную одной кнопкой
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Чеккаут репозитория
        uses: actions/checkout@v3

      - name: Настройка Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Установка зависимостей
        run: pip install -r requirements.txt

      - name: Запуск парсера
        run: python export_posts.py

      - name: Сохранение обновленного products.json
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add products.json
          git commit -m "auto: обновление каталога товаров" || exit 0
          git push
